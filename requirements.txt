# Dev/GPU venv shortcut: installs base deps + the cu130 torch stack.
#
#   python -m venv cc_agentic_env && source cc_agentic_env/bin/activate
#   pip install -r requirements.txt
#
# For a CPU-only venv, install the cpu variant instead:
#   pip install -r requirements/base.txt -r requirements/torch-cpu.txt
#
# The Dockerfile installs requirements/base.txt + the torch variant
# matching the TORCH_VARIANT build arg (cpu or cu130).

-r requirements/base.txt
-r requirements/torch-gpu.txt
