# Base Python dependencies. Torch is NOT here — installed separately so the
# same base works for both CPU and GPU images:
#   pip install -r requirements/base.txt -r requirements/torch-cpu.txt
#   pip install -r requirements/base.txt -r requirements/torch-gpu.txt
#
# Top-level requirements.txt includes this + torch-gpu for the existing
# venv workflow; the Dockerfile picks the variant via the TORCH_VARIANT
# build arg.

# --- Agentic framework (loose) ---
langgraph>=0.2.0
langchain>=0.1.0
langchain-core>=0.1.0

# --- Pinned model + utility stack ---
pydantic==2.12.5
numpy==2.3.3
scipy==1.16.3
accelerate==1.12.0
datasets==4.4.1
huggingface-hub==0.36.0
openai-whisper==20250625
openai>=1.0.0
peft==0.18.0
safetensors==0.7.0
tiktoken==0.12.0
tokenizers==0.22.1
transformers==4.57.3
audioread==3.1.0
decorator==5.2.1
filelock==3.19.1
joblib==1.5.2
librosa==0.11.0
packaging==25.0
platformdirs==4.5.0
pyarrow==22.0.0
pytz==2025.2
PyYAML==6.0.3
requests==2.32.5
soundfile==0.13.1
soxr==1.0.0
tqdm==4.67.1
typing_extensions==4.15.0
urllib3==2.5.0
einops
transformers_stream_generator
google-genai

# --- Preprocessing (channel split + Silero VAD) ---
silero-vad>=4.0

# --- Persistence (PostgreSQL) ---
psycopg[binary]>=3.1

# --- HTTP server (long-lived job API; alternative to the one-shot batch CLI) ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.9
