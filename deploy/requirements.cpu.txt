# CPU-only dependency set for the production container image.
#
# This mirrors requirements.txt but swaps the CUDA torch stack for the CPU
# builds and drops triton (a GPU-only compiler). The dev requirements.txt is
# unchanged — this file exists solely so the prod image (RHEL 9, CPU-only)
# installs a working torch without pulling ~2 GB of CUDA libraries it can't use.
#
# Built against the CPU wheel index:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r deploy/requirements.cpu.txt

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core framework (same as dev) ---
langgraph>=0.2.0
langchain>=0.1.0
langchain-core>=0.1.0

# --- Pinned (CPU torch stack; no +cu130, no triton) ---
pydantic==2.12.5
torch==2.9.1
torchaudio==2.9.1
torchvision==0.24.1
numpy==2.3.3
scipy==1.16.3
accelerate==1.12.0
datasets==4.4.1
huggingface-hub==0.36.0
openai-whisper==20250625
openai>=1.0.0
peft==0.18.0
safetensors==0.7.0
tiktoken==0.12.0
tokenizers==0.22.1
transformers==4.57.3
audioread==3.1.0
decorator==5.2.1
filelock==3.19.1
joblib==1.5.2
librosa==0.11.0
packaging==25.0
platformdirs==4.5.0
pyarrow==22.0.0
pytz==2025.2
PyYAML==6.0.3
requests==2.32.5
soundfile==0.13.1
soxr==1.0.0
tqdm==4.67.1
typing_extensions==4.15.0
urllib3==2.5.0
einops
transformers_stream_generator
google-genai

# --- Preprocessing ---
silero-vad>=4.0

# --- Persistence ---
psycopg[binary]>=3.1
